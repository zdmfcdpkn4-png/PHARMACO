# Mettre "false" pour brancher sur le vrai backend Express.
# Par défaut "true" : données mockées en mémoire (aucune base requise).
VITE_USE_MOCK=true

# URL de l'API (utilisée quand VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:4000/api
